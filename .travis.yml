language: go

install:
  - grep 'import' -rl ./| grep ".go"| xargs sed -i 's#"github.com/ontio/ontology/#"github.com/ontio/ontology/Ontology/#g'
  - go get github.com/Masterminds/glide
  - glide install

go:
  - 1.8.x
